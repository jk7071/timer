import React from 'react';
import Timer from './Timer'; // Import the Timer component

const App = () => {
  return (
    <div>
      <Timer /> {/* Display the Timer component */}
    </div>
  );
};

export default App;
